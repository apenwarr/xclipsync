#!/bin/sh
cd "$(dirname "$0")"

if [ -z "$DISPLAY" ]; then
    echo 'clipsync: $DISPLAY must be set.' >&2
    exit 99
fi

MAIN=":0"
ME="$DISPLAY"
while :; do
    DISPLAY=$MAIN ./xclipfrom "$ME"   || exit 1
    DISPLAY=$ME   ./xclipfrom "$MAIN" || exit 1
done
